{
    "version": 2,
    "builds": [
      { "src": "server.js", "use": "@now/node" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/$1" },
      { "src": "/app/(.*)", "dest": "/app/$1" },
      { "src": "/(.*)", "dest": "server.js" }
    ]
  }
  